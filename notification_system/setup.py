from setuptools import setup, find_packages

setup(
    name="notification_system",
    version="0.1.0",
    packages=find_packages(),
    install_requires=[],
    author="Your Name",
    description="A modular notification system using design patterns.",
    classifiers=[
        "Programming Language :: Python :: 3",
    ],
)
